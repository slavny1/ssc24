import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Spacer()
            Image("compass")
                .resizable()
                .padding()
                .scaledToFit()
            Spacer()
        }
    }
}
